# PROJECT BRAIN — bancovital-front
> Operating context for this project. Read this in full before any substantive work, then work strictly in line with it.
> Ground every action in this file and in the actual code. Do NOT invent conventions, file paths, APIs, or commands — if something is not documented here or verifiable in the code, say so instead of guessing.
> Auto-generated by /memorizar-proyecto. Last absorbed: 2026-06-16.

## 1. What this is
SaaS front-end for bancovital (lab billing/management). Next.js 16 + React 19, REST backend (NestJS). UI en español, code en inglés. Manages labs, orders, plans, billing, contracts, reservations, super admin powers (impersonation, metrics, audit logs). APP ÚNICA bancovital (des-slug completed 2026-06-15): no slug routing, no per-lab theming, JWT+labId isolation. Phase F5b in production (billing/account + announcements + checklist); F8 (redesign) pending. Vercel + Supabase Auth.

## 2. Stack & versions
- Next.js 16.2.6 (App Router, Server Components) + React 19.0.0
- Tailwind CSS v4 (CSS custom properties, light-only)
- Radix UI primitives (alert-dialog, checkbox, dialog, dropdown-menu, label, select, tabs) + class-variance-authority 0.7.1
- react-hook-form 7.54.2, Zod 3.24.1
- axios 1.7.9, TanStack Query 5.62.11
- Supabase SSR 0.5.2, @supabase/supabase-js 2.47.10
- lucide-react 0.469.0, sonner 1.7.1, decimal.js 10.6.0, signature_pad 5.x
- Biome 1.9.4 (lint/format), TypeScript 5.x, pnpm 9.x

## 3. How the code is organized
**Entry points:** `app/layout.tsx` root (global fonts, QueryProvider, Toaster). `app/globals.css` design tokens.

**Route segments (app única, no slug):**
- `app/(auth)/` — public: `/login`, `/auth/set-password`
- `app/(app)/` — protected (receptionist/bioquimico/admin): `/ordenes`, `/pacientes`, `/medicos`, `/obras-sociales`, `/practicas`, `/reportes`, `/admin`, `/configuracion`
- `app/(super)/super/` — protected (super role only): `/super`, `/super/announcements`, `/super/audit`, `/super/contracts`, `/super/labs`, `/super/labs/[id]/cuenta` (billing/account), `/super/metrics`, `/super/plans`, `/super/reuniones` (meetings)
- `app/contratar/[token]` — public contract signing (F3, live)
- `app/reunion/[token]` — public reunion RSVP (F4, live)
- `app/informe/[token]` — public patient report portal (F7, live)
- `app/` root & landing (anon, live)

**Components:**
- `components/ui/` — Radix primitives with CVA variants: button, input, textarea, label, card, dialog, alert-dialog, dropdown-menu, select, tabs, checkbox, form-field, pagination
- `components/domain/` — business: MoneyDisplay, forms, comboboxes, ConfirmDialog, OrderActions, etc.
- `components/layout/` — Sidebar (fixed 240px, hidden md:flex), Topbar (56px), PageHeader
- `components/providers/` — QueryProvider, etc.

**API & auth:**
- `lib/api/` — types.ts (manual DTO mirror), client.ts (browser axios + token from cookie), server.ts (RSC axios + token from session)
- `lib/auth/session.ts` — getSessionToken(), Supabase auth helpers
- `lib/impersonate.ts` — super impersonation (cookie + header forwarding to API)
- `proxy.ts` (Next 16 equivalent of middleware) — route protection (all non-public → /login), no tenant routing (app única)

**Assets & styles:**
- `app/globals.css` — design tokens (colors, spacing, typography, motion)
- `tailwind.config.ts` (stub, Next 16 auto-config)
- `public/` — favicon, static images
- `docs/` — detailed guides (01-STACK.md, 02-SETUP.md, PLAN-SAAS-FRONT.md, etc.)

## 4. Conventions (how code is actually written here)
- **Files:** kebab-case (e.g. `money-display.tsx`); **Components:** PascalCase exports; no barrel exports (import from direct path)
- **Imports:** `@/` absolute alias (rootDir), order: React/Next → libs → local (`@/lib`, `@/components`)
- **Server Components:** default export, no 'use client' tag; async/await OK, fetch in Server Components
- **Client Components:** 'use client' at top; useState for form/UI state only; no server-side fetches
- **Data flow:** getServerApi() in Server Components, pass data down as props to interactive client parts
- **Mutations:** useMutation from TanStack Query → onSuccess invalidateQueries + sonner toast + router.push redirect
- **State:** TanStack Query for remote async (caching, dedup), useState for ephemeral UI only (toggles, form draft)
- **API client:** apiClient (browser, JWT auto-from-cookie), getServerApi (server, token from session), both use extractApiError() helper
- **Types:** lib/api/types.ts manually mirrors backend DTOs (NOT auto-sync; update by hand on back changes)
- **Errors:** axios onError handlers; 401 → /login redirect via interceptor; 422 validation → message array display; 5xx → toast
- **UI:** Use cn() utility (clsx + tailwind-merge); token CSS vars (var(--color-*)); lucide icons h-4 w-4 default; no hardcoded colors
- **Forms:** react-hook-form + Zod, field-level error display, ConfirmDialog for destructive ops
- **Money:** decimal.js (match backend precision), MoneyDisplay component for formatting (tabular font, .toDisplay())
- **No testing:** no test framework in codebase; manual/e2e only

## 5. Design system — how to use each design
**Tokens** (app/globals.css @theme block — F8 Fase 0, marca Banco Vital FIJA): surfaces (--color-bg #f6f7fb, --color-bg-elevated, --color-bg-subtle, --color-scrim), borders (--color-border, --color-border-strong, --color-border-emphasis), text (--color-fg #1a2138, --color-fg-muted, --color-fg-subtle, --color-fg-inverse), **primary = navy #1f2b5b (FIJO, sin override por tenant; dominante en CTAs/activos/nav)**, **accent = rojo #cd0f0f (SOLO acento de marca: logo/detalles; NUNCA color de acción ni de error)**, semantic (success #15803d, warning #b45309, danger #b91c1c, info #0369a1), radii (sm/md/lg/pill), shadows (xs/sm/md/lg, navy-tinted), typography **Geist Sans (--font-sans) + Geist Mono (--font-mono) vía paquete `geist` en app/layout.tsx (NO next/font/google)**, motion (--ease-out-app/--ease-out-soft, --duration-fast/base/slow + keyframes .motion-* y guard prefers-reduced-motion). Reemplazó el placeholder teal "AV Diagnóstico". NOTA: docs/08-DESIGN-TOKENS.md y docs/11-PLAN-ACCION.md siguen citando Inter/teal — pendiente de la pasada de coherencia (parte 7).

**Primitives** (components/ui/): Button (variants: default, destructive, outline, secondary, ghost, link; sizes: default, sm, lg, icon; prop `loading` con spinner), Badge (cva; variants default/secondary/outline/accent + success/warning/danger/info), Input, Textarea, Label, Select, Checkbox, Card, Dialog, AlertDialog, Tabs, DropdownMenu, FormField (asocia error/ayuda al control vía aria-describedby/aria-invalid) (all Radix-based, CVA-typed). QA en `/showcase` (dev/preview; 404 en prod vía VERCEL_ENV).

**Domain** (components/domain/): MoneyDisplay (ARS formatter w/ tabular font), forms (PatientForm, DoctorForm, OrderForm, etc.), comboboxes (PatientCombobox, DoctorCombobox, NbuGrid for multi-select), ConfirmDialog (destructive actions), status badges (OrderStatusPill, etc.).

**Color mapping:** primary (CTAs, active states), danger (delete/cancel/destructive), success (confirmed/normal/passed), warning (in-progress/attention), info (informational), opacity 50 (disabled/inactive). Soft variants for backgrounds. Foreground text always fg/fg-inverse.

**Layout:** Sidebar fixed left 240px hidden md:flex (sticky topbar inside, nav links); Topbar 56px right-aligned (breadcrumb, user menu); Main max-w-6xl mx-auto px-6 py-8 (responsive).

**Strict rules:** (1) Never hardcode colors — use CSS vars. (2) Never <img> with Tailwind utilities (use Image component if Next optimization needed). (3) Tables use .tabular monospace for numbers. (4) Toasts only sonner (never alert()). (5) Icons h-4 w-4 default, override size:icon variant. (6) No dark mode (light-only). (7) Medical blue for primary, never generic brand colors.

## 6. Commands & workflows
**Setup:** `pnpm install`, `cp .env.example .env.local` (complete with real values), `pnpm dev` (http://localhost:3000).

**Dev:** 
- `pnpm dev` (:3000, hot reload)
- `pnpm typecheck` (tsc --noEmit)
- `pnpm lint` (Biome check)
- `pnpm lint:fix` (Biome check + autofix)
- `pnpm build` (prod build, 60-90s)
- `pnpm start` (local prod server)
- `pnpm clean` (rm .next .turbo cache)

**Git:** Always work on `dev` branch. `git checkout dev && git pull --ff-only origin dev` before edits. Push to `origin/dev` when ready (no PR to dev; Mateo merges dev→main manually). Never commit/push `main` directly. Undo: `git stash -u && git checkout dev && git pull && git stash pop`.

**Env vars (.env.local, NEVER commit):**
- NEXT_PUBLIC_API_URL — backend base URL (must include /api; e.g. https://api.lab.local/api)
- NEXT_PUBLIC_APP_URL — frontend base URL (e.g. http://localhost:3000)
- NEXT_PUBLIC_SUPABASE_URL — Supabase project URL (https://xxx.supabase.co)
- NEXT_PUBLIC_SUPABASE_ANON_KEY — anon public key (safe, RLS protects data)
- NEXT_PUBLIC_POSTHOG_KEY (optional) — analytics
- NEXT_PUBLIC_SENTRY_DSN (optional) — error tracking
- Secrets (server-only, no NEXT_PUBLIC_): RESEND_API_KEY, GOOGLE_OAUTH_*

Critical: Rebuild after NEXT_PUBLIC_* changes (env embedded at build time). Never expose SUPABASE_SERVICE_ROLE_KEY with NEXT_PUBLIC_ prefix.

**Deploy:** Push `dev` → Vercel Preview (staging). Mateo merges `dev→main` → Vercel Production. NEXT_PUBLIC_* vars configured in Vercel project settings per environment (Production/Preview/Development). Lab backend must have frontend domain in CORS_ALLOWED_ORIGINS.

## 7. Decisions & why
- **REST not tRPC:** simpler backend, manual DTO sync cost acceptable, easier to onboard. Type safety traded for simplicity.
- **Radix UI standalone (no shadcn registry):** lighter bundle, full control, zero lock-in to third-party component library updates.
- **axios + TanStack Query:** familiar, powerful caching/dedup, optimistic updates, great devx.
- **Supabase Auth:** RLS-safe (anon key public), SSR-safe (@supabase/ssr), JWT role-aware, easy multi-tenant.
- **Biome single tool:** fast, good defaults, replaces ESLint + Prettier, config-free for this project.
- **Server Components default:** Next.js 16 best practice, reduces JS bundle, simpler data flow.
- **CSS custom properties:** consistent theming, override per tenant (F1), smaller CSS than Tailwind config overrides.
- **decimal.js:** match backend precision, prevent float rounding errors on money/decimals.
- **Light-only theme:** professional medical application, no dark mode complexity.
- **Manual type mirror:** avoids monorepo/shared package complexity; cost is manual sync on back DTOs change (annotate in PR).

## 8. Do's & Don'ts
**DO:**
- Read this brain IN FULL before work. It's not optional.
- Server Components by default; 'use client' only when interactive (forms, useState, browser APIs).
- Fetch in Server Components via getServerApi(), pass data to clients as props.
- Invalidate queries after mutations (revalidate stale data).
- Update lib/api/types.ts when backend DTO changes (annotate in backend PR).
- Use cn() to merge Tailwind classes (clsx + tailwind-merge).
- Test locally before pushing: `pnpm typecheck && pnpm lint && pnpm dev` (check the UI).
- Kebab-case files, PascalCase components. No barrel exports.
- Commit `dev` branch, push `origin/dev`. No direct main commits.
- Use monospace (tabular) font for numbers/money in tables/cards (CSS class .tabular).
- Sonner toast for feedback (success, error, info, loading).

**DON'T:**
- Commit .env.local, .env, .env.example. Mateo explicitly bans .env.example in repos.
- Hardcode colors. Use CSS vars (var(--color-primary), etc.).
- Use localStorage without SSR check (causes hydration mismatch).
- Mutate API response objects. Copy & update immutably.
- Call apiClient in Server Components (will fail; use getServerApi instead).
- Fetch in client components without TanStack Query (causes rerenders, no caching).
- Skip Biome/typecheck before push. Merges always lint-pass.
- Push `main`. Ever.
- Invent paths, file names, or API endpoints not documented here or in code.
- Create <img> tags with Tailwind classes (causes next/image breakage or unoptimized images).
- Use dark mode tokens or hardcode dark colors.

## 9. Current state
**Branch:** dev. **Last commit (dev):** `00876e4 docs(des-slug): banner de cambio de modelo en PLAN-SAAS-FRONT (app única, sin slug)`. **Phase:** DES-SLUG shipped 2026-06-15 (app única bancovital, no slug routing, JWT+labId isolation). F5b shipped (super billing, account page, announcements, tenant banner). F4 (landing + booking) live. F3 (contracts + signing) live. F7 (patient report portal) live. **PRs/Issues:** none open. **Uncommitted changes:** none (clean working tree).

**Timeline (last 3 days):**
- 2026-06-15: DES-SLUG shipped (app única; banner en PLAN-SAAS-FRONT documenting modelo)
- 2026-06-13: F5a shipped (impersonation + metrics + audit log viewer + sidebar respects impersonation)
- 2026-06-13: F4 shipped (public landing + booking flow + super reuniones list)
- 2026-06-12: F3 shipped (public contract signing page + super contracts panel)

**Next:** F8 full redesign (Nodo branding, impeccable polish, sistema=Banco Vital). F6 (admin personalization per lab). F2 (plans UI integration).

## 10. Gotchas & landmines
- **Type sync manual:** update lib/api/types.ts by hand when backend DTO changes; no auto-generation. Annotate PR in backend repo.
- **NEXT_PUBLIC_API_URL must include /api:** e.g. `https://api.lab.local/api`, not `https://api.lab.local` (endpoints are GET /api/ordenes, not GET /ordenes).
- **Middleware (proxy.ts) ~50-150ms latency:** Supabase JWT validation on every request. Optimize via KV cache if needed (F9+).
- **No dark mode:** tokens defined light-only; don't add dark: variants.
- **decimal.js precision 28 globally:** match backend; keep money as strings in JSON wire format, never JSON.stringify(new Decimal()).
- **Supabase RLS:** anon key is public-safe (RLS protects); NEVER expose service_role key with NEXT_PUBLIC_ prefix.
- **Next.js 16 breaking changes:** read https://nextjs.org/docs before writing unfamiliar APIs. Server Components vs Client different rules.
- **Query staleTime:** default 30s (refetch if older); configure per useQuery if needed.
- **Lab branding server-side:** /lab-config endpoint blocks page load; cache aggressively or move to layout-level fetch.
- **App única (DES-SLUG):** No slug routing. Routing is flat. Authorization = JWT labId + session. No per-lab theming (light-only, bancovital branding fixed).
- **CORS required:** backend must list frontend domain (Vercel URL + localhost:3000 for dev) in CORS_ALLOWED_ORIGINS.
- **Signature_pad:** canvas-based, needs 'use client'; initialize in useEffect after hydration to avoid SSR mismatch.
- **React 19 hooks:** use latest TanStack Query v5, avoid deprecated useFetcher etc.

## Manual notes
(Preserved from previous runs; updated by human review.)
