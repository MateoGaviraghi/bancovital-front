// Login migrated to /[slug]/login — this route is no longer active.
// Kept as a placeholder. Will be removed in a future cleanup.

export default function LegacyLoginPage() {
  return (
    <div className="text-[var(--color-fg-muted)] text-sm p-8 text-center">
      Ingresá desde la URL de tu laboratorio (p. ej., /mi-lab/login).
    </div>
  );
}
